subprojects {

    repositories {
        mavenCentral()
    }

    apply plugin: 'maven'
    apply plugin: 'groovy'
    apply plugin: 'idea'

    group = 'org.gradle.plugins'
    version = "${currentVersion}"
}

boolean isSnapshot() {
    currentVersion.endsWith('SNAPSHOT')
}
