machine:
  environment:
    TERM: dumb

dependencies:
  cache_directories:
    - "~/.gradle"
  override:
    - ./gradlew installWrappers -s
    - ./gradlew dependencies


test:
  override:
    - ./gradlew check