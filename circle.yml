machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb
    GRADLE_OPTS: -Xmx1024m

dependencies:
  override:
    - ./gradlew installWrappers dependencies -Dscan

test:
  override:
    - ./gradlew check -Dscan

general:
  branches:
    ignore:
      - gh-pages