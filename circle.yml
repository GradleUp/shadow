machine:
  environment:
    TERM: dumb
    GRADLE_OPTS: -Xmx1024m

dependencies:
  override:
    - ./gradlew installWrappers dependencies -Dscan -Dorg.gradle.daemon=false

test:
  override:
    - ./gradlew check -Dscan -Dorg.gradle.daemon=false

general:
  branches:
    ignore:
      - gh-pages