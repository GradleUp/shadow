machine:
  environment:
    TERM: dumb
    GRADLE_OPTS: -Xmx256m

dependencies:
  cache_directories:
    - "~/.gradle"
  override:
    - ./gradlew installWrappers -s
    - ./gradlew dependencies


test:
  override:
    - ./gradlew check